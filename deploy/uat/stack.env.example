# Docker registry host without protocol
REGISTRY_HOST=registry.example.com
# Registry namespace or repository prefix
REGISTRY_NAMESPACE=hr-cms
# Image tag to deploy. Jenkins overwrites this with the build-specific tag.
IMAGE_TAG=uat

# Host port bindings
API_PORT=4000
WEB_PORT=8080
